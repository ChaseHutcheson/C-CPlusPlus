#include <stdio.h>

int main()
{
    // Bitwise operators are special operators used in bit level programming

    // & = AND
    // | = OR
    // ^ = XOR
    // << = Left Shift
    // >> = Right

    return 0;
}
